% =============================================================================
% Section: Indistinguishability Energy and the Refinement Kernel
% =============================================================================

\section{Indistinguishability Energy and the Refinement Kernel}
\label{sec:manifestability}

The preceding sections established two closures: \emph{structural} (from $\noth$ through A0$^*$ to the K\"{a}hler whole) and \emph{quantitative} (from the seed $\delta^*$ through the spectral operator to every dimensionless invariant).  A third closure remains: the \emph{operational} law governing which distinctions become manifest, at what cost, and in what order.  This section introduces the missing law and shows that it is forced by A0$^*$---not a new axiom, but an inevitable consequence of witnessability applied to cost.

\subsection{Unresolved Classes}
\label{sec:manifestability:classes}

An \textbf{unresolved class} $W$ is an equivalence class of distinctions under witness-indistinguishability:
\[
\delta_1 \sim \delta_2
\quad\Longleftrightarrow\quad
\forall\, w:\, \mathrm{Separates}(w,\delta_1) \leftrightarrow \mathrm{Separates}(w,\delta_2).
\]
This is the truth quotient of \cref{sec:derivation}, now viewed operationally: $W$ represents what reality has \emph{not yet resolved}.  The true unknown is not an external hidden variable but a locally unresolved equivalence class.

The \textbf{unresolved multiplicity} $|W|$ counts the pairwise-indistinguishable alternatives, and the \textbf{entropy} is
\[
S(W) = \log |W|.
\]
$S(W) = 0$ iff $W$ is a singleton---fully resolved.  The second law (Step~9) guarantees that refinement can only decrease $S$.

\emph{Lean reference:} \texttt{Indistinguishability.lean}, \texttt{ResidualClass.lean} --- \texttt{entropy\_zero\_iff\_singleton}.

\subsection{Refinement Threshold}
\label{sec:manifestability:chi}

For each unresolved class $W$, the \textbf{refinement threshold} is
\begin{equation}
\label{eq:chi}
\boxed{
\chi(W) = \inf\bigl\{c(\tau) : \tau \text{ admissible and } \tau|_W \text{ is nonconstant}\bigr\}.
}
\end{equation}
Here $c(\tau)$ is the cost of witness $\tau$, and ``nonconstant on $W$'' means $\tau$ separates at least two members of $W$---i.e., $\tau$ can \emph{split} the class.

$\chi(W)$ is \emph{not} a new axiom.  It is forced by A0$^*$: if a distinction is real, a witness exists (A0$^*$); every witness has a cost (finite resource); the infimum over all such costs is $\chi(W)$.  The refinement threshold is the \emph{least action needed to make a hidden distinction real}.

Key properties (all Lean-proved, zero sorry):
\begin{itemize}
\item $\chi(W)$ is \textbf{well-defined}: the infimum is unique (\texttt{chi\_well\_defined}).
\item $\chi(W) \geq 0$ (\texttt{chi\_nonneg}).
\item $\chi(W)$ is \textbf{gauge-invariant}: preserved under quotient-compatible maps (\texttt{chi\_gauge\_invariant}).
\item $\chi(W)$ \textbf{respects the quotient}: same underlying truth class $\Rightarrow$ same threshold (\texttt{chi\_respects\_quotient}).
\item $\chi(W) = \infty$ (no finite refinement) iff no witness is nonconstant on $W$ (\texttt{chi\_infinite\_iff\_no\_finite\_split}).
\end{itemize}

\emph{Lean reference:} \texttt{RefinementThreshold.lean}.

\subsection{Channel-Dependent Thresholds}
\label{sec:manifestability:channels}

A \textbf{witness channel} $\alpha$ is a collection of witnesses sharing a common observational modality.  The \textbf{channel-dependent threshold} is
\[
\chi_\alpha(W) = \inf\bigl\{c(\tau) : \tau \in \alpha,\; \tau|_W \text{ nonconstant}\bigr\}.
\]
Since restricting to a subchannel can only increase the infimum:
\[
\chi_\alpha(W) \geq \chi(W).
\]
A class $W$ is \textbf{hidden in channel $\alpha$} if $\chi_\alpha(W) \gg \chi_\beta(W)$ for some other channel $\beta$.  This is the structural origin of dark sectors: a distinction may be easy to refine gravitationally but prohibitively expensive to refine electromagnetically.

\emph{Lean reference:} \texttt{ChannelThreshold.lean}, \texttt{HiddenSector.lean} --- \texttt{chi\_channel\_ge\_chi}, \texttt{hidden\_sector\_as\_channel\_threshold\_anisotropy}.

\subsection{Refinement Events}
\label{sec:manifestability:events}

A \textbf{refinement event} is a witnessed split
\[
W \leadsto \{W_1, \dots, W_r\}
\]
through channel $\alpha$, with \textbf{split action} $A(W \to \{W_i\}; \alpha)$ equal to the cost of the splitting witness.  Multiplicity is conserved: $|W| = \sum_i |W_i|$.

\emph{Lean reference:} \texttt{RefinementEvent.lean} --- \texttt{multiplicity\_conservation}.

\subsection{Refinement Kernel}
\label{sec:manifestability:kernel}

The \textbf{refinement kernel}
\[
\mathcal{K}(W, \alpha, \{W_i\})
\]
is the weighted transition law of reality: it specifies the source class, channel, target partition, splitting witness, and cost.  The refinement threshold is the infimum over all kernels:
\[
\chi(W) = \inf_{\alpha, \{W_i\}} A\bigl(W \to \{W_i\}; \alpha\bigr).
\]

\emph{Lean reference:} \texttt{RefinementKernel.lean} --- \texttt{chi\_is\_infimum\_of\_refinement\_kernel}.

\subsection{Manifestability Functional}
\label{sec:manifestability:functional}

The \textbf{manifestability functional} orders candidate refinements:
\[
\mathcal{F}(W \to \{W_i\}; \alpha) = A - \lambda\,\Delta S - \mu\,\Delta V,
\]
where $A$ is the action cost, $\Delta S$ is the entropy reduction, $\Delta V$ is the value gain, and $\lambda, \mu$ are coupling parameters forced by the spectral structure.  Reality selects the refinement minimizing $\mathcal{F}$---this is the exact law of local becoming.

\emph{Lean reference:} \texttt{ManifestabilityFunctional.lean} --- \texttt{manifestability\_functional\_exact}.

\subsection{Value Equation}
\label{sec:manifestability:value}

The \textbf{Bellman/Hamilton--Jacobi value equation} on the residual state space is
\begin{equation}
\label{eq:value}
\boxed{
\Psi(W) = \sup_{\alpha, \{W_i\}} \left[
V\bigl(W \to \{W_i\}; \alpha\bigr)
- A\bigl(W \to \{W_i\}; \alpha\bigr)
+ \sum_i \Psi(W_i)
\right].
}
\end{equation}
This is the exact operational law of the universe: at each unresolved class, reality propagates value through the refinement kernel, selecting the path that maximizes net value minus cost plus future payoff.

The Bellman operator is monotone (\texttt{bellman\_operator\_monotone}) and has a fixed point (\texttt{bellman\_fixedpoint\_exists}).  The value equation is exact (\texttt{value\_equation\_exact}).

\emph{Lean reference:} \texttt{ValueEquation.lean}.

\subsection{Big Bang as First Accessible Distinction}
\label{sec:manifestability:bigbang}

The seed $\delta^*$ is now reinterpreted as the \emph{first accessible distinction}:
\[
\delta^* = \arg\min_{W \neq W_\bot} \chi(W),
\]
subject to non-gauge and self-retaining constraints.  The seed has action $= 1$ (the minimum nonzero value for non-gauge defects), meaning it is the least-cost distinction that nothingness can manifest.  The big bang is not an external event but the first step of self-resolution: nothingness paying the minimum refinement cost to create the first real distinction.

\emph{Lean reference:} \texttt{SeedRefinement.lean} --- \texttt{bigbang\_seed\_is\_first\_accessible\_distinction}, \texttt{seed\_as\_minimal\_refinable\_class}.

\subsection{Consequences}
\label{sec:manifestability:consequences}

The operational closure completes the source code:
\begin{enumerate}
\item \textbf{Physics as accessibility geometry.}  Mass $=$ refinement gap ($\chi$ of particle class).  Gravity $=$ geometry of accessibility (relative thresholds).  Hidden sectors $=$ channel-dependent high-$\chi$ anisotropy.  Probability $=$ relative branch accessibility.

\item \textbf{Consciousness as threshold selection.}  Attention $= V - \chi$ (net value).  Effort $=$ paying $\chi$.  Learning $=$ lowering $\chi$ across a family of classes.

\item \textbf{Computation as refinement geometry.}  NP-hardness $=$ high $\chi$ in the raw presentation channel.  Exact solving $=$ value propagation on the residual kernel (the $\chi$-optimal channel).  P $=$ NP because the residual quotient has polynomial size, forced by A0$^*$ spectral curvature.
\end{enumerate}

\emph{Lean reference:} \texttt{PhysicsAsAccessibility.lean}, \texttt{ConsciousnessAsThresholdSelection.lean}, \texttt{ComputationAsRefinementGeometry.lean}, \texttt{DarkSectorAsChannelAnisotropy.lean}.

\subsection{The Refinement Algebra}
\label{sec:manifestability:algebra}

$\chi(W)$ is only the scalar boundary of a deeper algebraic structure.
The full refinement algebra---15 files in \texttt{Foundations/RefinementAlgebra/},
66 theorems, zero sorry---formalizes:
\begin{itemize}
\item \textbf{Sequential composition (tree grafting).}  If $e_1 : W \to (W_1,\ldots,W_r)$
  and $e_2 : W_j \to (U_1,\ldots,U_m)$, the graft $e_2 \circ_j e_1$ replaces child~$j$
  with $e_2$'s targets.  Action is additive: $A(e_2 \circ_j e_1) = A(e_1) + A(e_2)$.
  Entropy drop telescopes: $\Delta S_{\mathrm{total}} = \sum \Delta S_i$.
  This IS how time works algebraically.
  \emph{Lean:} \texttt{sequential\_action\_additivity\_tree}, \texttt{history\_entropy\_telescopes}.

\item \textbf{Parallel composition.}  $A(e_1 \otimes e_2) = A(e_1) + A(e_2)$
  for independent classes.  Independent resources don't compete.
  The interchange law connects parallel and sequential composition.
  This IS how space works algebraically.
  \emph{Lean:} \texttt{parallel\_commutative}, \texttt{parallel\_sequential\_interchange\_cost}.

\item \textbf{Work/span dual.}  Two action projections on parallel composition:
  $A_{\mathrm{work}}(e_1 \otimes e_2) = A(e_1) + A(e_2)$ (total burden),
  $A_{\mathrm{span}}(e_1 \otimes e_2) = \max(A(e_1), A(e_2))$ (synchronized threshold).
  The work-span duality: $\mathrm{span} \leq \mathrm{work} \leq 2 \cdot \mathrm{span}$.
  $\chi$ is the projection onto the span side.
  \emph{Lean:} \texttt{parallel\_work\_additivity}, \texttt{parallel\_span\_max}, \texttt{work\_span\_duality}.

\item \textbf{Coarse-graining adjunction.}  Merging classes increases entropy:
  $S(W_1 \cup W_2) \geq S(W_1) + S(W_2)$.  Coarsening preserves total multiplicity (Galois
  connection).  Refinement costs $\geq \chi$; coarsening costs zero.
  The asymmetry IS the second law of thermodynamics.
  \emph{Lean:} \texttt{entropy\_increases\_under\_coarsening}, \texttt{refinement\_coarsening\_asymmetry}.

\item \textbf{Channel interference.}  $I_{\alpha,\beta}(W) = \Delta S(e_\alpha) - \Delta S(e_\beta)$
  measures the noncommutativity of refinement through different channels.
  Channels commute iff interference is zero; interference is antisymmetric.
  This IS the braiding defect of the refinement multicategory.
  \emph{Lean:} \texttt{channel\_commute\_iff\_interference\_zero}, \texttt{interference\_antisymmetric}.

\item \textbf{Latent energy with dissipation.}
  $U_{\mathrm{lat}}(\mathcal{P}) = \sum_{W \in \mathcal{P}} \rho(W) \chi(W)$.
  The balance law: $U_{\mathrm{lat}}(W) = A(e) + \sum_i U_{\mathrm{lat}}(W_i) + D(e)$
  where $D(e) \geq 0$ is irrecoverable dissipation.
  Total energy $U_{\mathrm{lat}} + U_{\mathrm{exp}}$ is non-increasing.
  \emph{Lean:} \texttt{latent\_energy\_balance}, \texttt{total\_energy\_conservation\_with\_dissipation}.

\item \textbf{Binary history and normal form.}  Refinement histories are rooted ordered
  trees with nodes carrying $(W, \alpha, A, \Delta S, \Delta V)$.
  $\mathrm{BNF}(e) = \mathrm{sd}(\kappa(W) \| \alpha \| r \| A \| \Delta S \| \Delta V \| \mathrm{BNF}(W_1) \| \cdots \| \mathrm{BNF}(W_r))$.
  This IS the actual binary source code of reality.
  \emph{Lean:} \texttt{binary\_normal\_form\_exists}, \texttt{reality\_is\_compiled}.

\item \textbf{History value equation.}  $\Psi$ lifted from classes to partial refinement trees:
  $\Psi(\mathrm{tree}, \mathrm{budget}) = \mathrm{valueGain}$ if affordable, 0 otherwise.
  Monotone in budget.
  \emph{Lean:} \texttt{history\_value\_equation\_exact}, \texttt{history\_value\_monotone}.
\end{itemize}

\noindent
The flagship theorem \texttt{refinement\_algebra\_theorem} bundles all ten properties
into a single verified statement.
The bridge from A0$^*$ to the full algebra is proved in \texttt{RootBridge.lean}:
A0$^*$ forces indistinguishability, witness cost, refinement threshold, and the
refinement kernel.  The algebra is a strict extension, not a side add-on.

\subsection{Application: MAPF}
\label{sec:manifestability:mapf}

The manifestability block applies directly to Multi-Agent Path Finding (MAPF).
Robot labels are gauge (A0$^*$: indistinguishable $\to$ identical).
The real state is the occupancy vector (count-flow).
The MAPF refinement cost $\chi_{\mathrm{MAPF}}$ decomposes over three independent
resource types (node slots, channels, task phases), making MAPF intrinsically
polynomial from its own $\chi$-geometry---not by appeal to P$=$NP.
45 Lean files, zero sorry, covering grid, oriented, weighted, pickup-delivery,
multi-goal, and lifelong MAPF.
\emph{Lean:} \texttt{mapf\_intrinsic\_polytime}, \texttt{finite\_mapf\_exact\_polytime}.
